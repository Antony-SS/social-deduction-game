{
    "name": "social-deduction-game",
    "private": true,
    "workspaces": ["client", "server"],
    "scripts": {
      "dev": "npm --workspace client run dev && uvicorn server.main:sio_app --reload --port 8080"
    },
    "devDependencies": {
      "concurrently": "^9.2.0"
    }
  }